/**
 * Function for displaying info in console with prefix - ENGINE: ...
 * @Param value - message that you want to display in console
 * */
export const engineNotif = (value: any): void => {
    console.log('ENGINE: ', value);
}