package constants.actors.constants;

public enum ActorTypes {
    // generally for actor which cannot be destroyed or transformed - e.g. houses, market
    STATIC,
    // for trees, resources, mobs, player, furnishes...
    DYNAMIC
}
